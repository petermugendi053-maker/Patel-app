# MT5 Assistant

Android Jetpack Compose dashboard for network/device diagnostics.

## Build
Open the project in Android Studio and sync Gradle, then build the debug APK.

GitHub Actions workflow is included at `.github/workflows/build-apk.yml`.
